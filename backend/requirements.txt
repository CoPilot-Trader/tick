# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database (if needed)
# sqlalchemy==2.0.23
# alembic==1.12.1

# HTTP Client
httpx==0.25.2
requests==2.31.0

# Data Processing
pandas==2.1.3
numpy==1.26.2
scipy==1.11.4
yfinance==0.2.28  # For OHLCV data fallback
python-dateutil==2.8.2  # For date parsing in level projection

# Machine Learning (common dependencies)
scikit-learn==1.3.2
lightgbm==4.1.0  # For ML-based future level prediction (optional - graceful fallback if not available)
xgboost==2.0.3  # Alternative ML library (optional - graceful fallback if not available)

# Environment & Config
python-dotenv==1.0.0

# Logging
loguru==0.7.2

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
httpx==0.25.2  # For testing FastAPI

# Code Quality
black==23.11.0
flake8==6.1.0
mypy==1.7.1

# Agent-specific dependencies should be added to individual agent requirements.txt files

# News Agent dependencies
# requests is already included above
# Note: sentence-transformers is optional (for semantic caching)
# sentence-transformers>=2.2.0  # Uncomment if using semantic caching
